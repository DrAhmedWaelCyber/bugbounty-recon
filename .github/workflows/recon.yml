# ⚠️  DEPRECATED — BugBounty-Recon v1 Workflow
# ================================================
# Developer : Ahmed Wael
#
# This workflow has been superseded by the v2 architecture.
# Please use the following workflows instead:
#
#   .github/workflows/weekly_split.yml  — Weekly wildcard split (runs every Monday)
#   .github/workflows/daily_recon.yml   — Daily reconnaissance (runs every day)
#
# This file is kept for reference only and is intentionally disabled.
# ================================================

name: "[DEPRECATED] BugBounty Recon v1"

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: "This workflow is deprecated. Use daily_recon.yml instead."
        required: true
        default: "abort"

jobs:
  deprecated_notice:
    name: "This workflow is deprecated"
    runs-on: ubuntu-latest
    steps:
      - name: Print deprecation notice
        run: |
          echo "============================================================"
          echo "  ⚠️  DEPRECATED WORKFLOW"
          echo "  This recon.yml has been replaced by:"
          echo "    - weekly_split.yml  (weekly wildcard split)"
          echo "    - daily_recon.yml   (daily reconnaissance)"
          echo "  Developer: Ahmed Wael"
          echo "============================================================"
          exit 1